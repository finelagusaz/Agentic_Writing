# Agentic Writing — プロジェクト記憶

## 動作環境

### Windows環境
- `TeamCreate` / `SendMessage` 等のエージェントチーム機能が**使用不可**
- 代替手段として `Agent` ツールによるサブエージェント直接スポーンで対処済み（第1話まで動作確認済み）
- Bash コマンド類（rm, mkdir 等）は影響なし

### macOS環境
- 全機能（チーム機能含む）が正常動作

## 進捗
- セットアップ（/setup-world, /setup-arc）〜第1話執筆まで動作確認済み
